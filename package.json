{
    "name": "armorylinks",
    "version": "1.0.0",
    "author": "armorynode",
    "private": true,
    "scripts": {
        "test": "sass --version",
        "build": "npm test && sass styles/index.scss:wwwroot/css/index.min.css --style compressed --no-source-map"
    }
}